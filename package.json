{
  "devDependencies": {
    "@types/node": "^15.6.0",
    "gts": "^3.1.0",
    "typescript": "^4.2.4",
    "yarn-upgrade-all": "^0.5.4"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  }
}
